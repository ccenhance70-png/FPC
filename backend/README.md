# Backend - Notes Service

Spring Boot 3 (Java 17) starter project for the Notes service.

## Run locally

1. Update `src/main/resources/application.yml` with your MySQL connection details.
2. Build and run:
   ```bash
   mvn spring-boot:run
   ```

## Entry point
- `src/main/java/com/example/notes/NotesApplication.java`
